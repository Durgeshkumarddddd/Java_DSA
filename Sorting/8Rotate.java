 
 // Given a rotated sorted array we have to find out the index of the minimum element if follow the o indexing
 class RotatedSortedArray {
 static int findMinIndex(int arr, int start, int end) {
      

 }

    public static void main(String[] args){
        int[] arr = {5, 6, 7, 8, 9, 10, 1, 2, 3, 4};
        int minIndex = findMinIndex(arr, 0, arr.length-1);
        System.out.println("Index of minimum element is: " + minIndex);
    }
}
